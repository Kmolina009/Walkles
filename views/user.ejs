<!DOCTYPE html>
<html>
  <head>
    <title>Walkles</title>
    <link href="https://fonts.googleapis.com/css?family=Permanent+Marker|Rokkitt&display=swap" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
          <ul>
            <li>
            <% if (user) { %>
             <a href="/logout">Log Out</a>
             <% } else { %>
              <a href="/auth/google">Login with Google</a>
               <% } %> 
             </li>
            </ul> 
    </header>
    <div>
     <%if (user._id.equals (user && user._id) ){%>
      <div class= "card-action">
        <form action="/dogs" method="POST">
          <input type="text" name="text" class="white-text">
          <button type="submit class="add-btn>Add Dog</button>
        </form>
      </div>
    </div>

    <p>USER PAGE</p>


  </body>
</html>
